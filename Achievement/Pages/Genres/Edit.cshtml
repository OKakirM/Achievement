@page
@model Achievement.Pages.Genres.EditModel

@{
    ViewData["Title"] = "Editar Género";
}

<div class="auth-wrap">
    <div class="auth-card">
        <h1 class="auth-title">Editar Género</h1>
        <p class="auth-subtitle">Atualiza o nome do género</p>

        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Genre.Id" />

            <div class="form-group">
                <label asp-for="Genre.Name" class="control-label"></label> <span class="req">*</span>
                <input asp-for="Genre.Name" class="form-control" aria-required="true" />
                <span asp-validation-for="Genre.Name" class="text-danger"></span>
            </div>

            <div class="form-actions">
                <a asp-page="Index" class="btn btn-secondary">Cancelar</a>
                <button type="submit" class="btn-accent">Guardar</button>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
