@page "/Games/Edit/{id:int}/{slug?}"
@model Achievement.Pages.Games.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Game</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Game.Id" />

            <input type="hidden" asp-for="Game.CoverImage" />
            <input type="hidden" asp-for="Game.BannerImage" />

            <input type="hidden" asp-for="RemoveCoverImage" id="RemoveCoverImageInput" />
            <input type="hidden" asp-for="RemoveBannerImage" id="RemoveBannerImageInput" />

            <div class="form-group">
<span class="text-danger">*</span>
                <label asp-for="Game.Name" class="control-label"></label>
                <input asp-for="Game.Name" class="form-control" aria-required="true"/>
                <span asp-validation-for="Game.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
<span class="text-danger">*</span>
                <label asp-for="Game.Description" class="control-label"></label>
                <input asp-for="Game.Description" class="form-control" aria-required="true"/>
                <span asp-validation-for="Game.Description" class="text-danger"></span>
            </div>
            <div class="form-group">
<span class="text-danger">*</span>
                <label asp-for="Game.ReleaseDate" class="control-label"></label>
                <input asp-for="Game.ReleaseDate" class="form-control" aria-required="true"/>
                <span asp-validation-for="Game.ReleaseDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Game.Rating" class="control-label"></label>
                <input asp-for="Game.Rating" class="form-control" />
                <span asp-validation-for="Game.Rating" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Game.Length" class="control-label"></label>
                <input asp-for="Game.Length" class="form-control" />
                <span asp-validation-for="Game.Length" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="SelectedPlatformIds" class="control-label">Plataformas</label>
                <select asp-for="SelectedPlatformIds" asp-items="Model.PlatformsList" class="form-control" multiple size="6">
                </select>
                <small class="form-text text-muted">Use Ctrl (ou Cmd) + clique para selecionar várias.</small>
                <span asp-validation-for="SelectedPlatformIds" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="SelectedGenreIds" class="control-label">Géneros</label>
                <select asp-for="SelectedGenreIds" asp-items="Model.GenresList" class="form-control" multiple size="6">
                </select>
                <small class="form-text text-muted">Use Ctrl (ou Cmd) + clique para selecionar várias.</small>
                <span asp-validation-for="SelectedGenreIds" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label>Imagem de Capa</label>
                <div id="coverPreviewWrapper" class="mb-2" style="@(string.IsNullOrEmpty(Model.Game.CoverImage) ? "display:none;" : "")">
                    <img id="coverPreview" src="@Url.Content("~/" + Model.Game.CoverImage)" style="max-width:200px;" class="d-block mb-2" />
                    <button type="button" id="removeCoverBtn" class="btn btn-sm btn-outline-danger">Remover imagem</button>
                </div>
                <p id="coverRemovedMsg" class="text-muted" style="display:none;">
                    A imagem será substituída pela imagem padrão ao guardar.
                    <button type="button" id="undoRemoveCoverBtn" class="btn btn-sm btn-link p-0">Desfazer</button>
                </p>

                <label asp-for="CoverImageFile" class="control-label"></label>
                <input asp-for="CoverImageFile" class="form-control" id="CoverImageFileInput" />
                <span asp-validation-for="CoverImageFile" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label>Imagem do Banner</label>
                <div id="bannerPreviewWrapper" class="mb-2" style="@(string.IsNullOrEmpty(Model.Game.BannerImage) ? "display:none;" : "")">
                    <img id="bannerPreview" src="@Url.Content("~/" + Model.Game.BannerImage)" style="max-width:400px;" class="d-block mb-2" />
                    <button type="button" id="removeBannerBtn" class="btn btn-sm btn-outline-danger">Remover imagem</button>
                </div>
                <p id="bannerRemovedMsg" class="text-muted" style="display:none;">
                    A imagem será substituída pela imagem padrão ao guardar.
                    <button type="button" id="undoRemoveBannerBtn" class="btn btn-sm btn-link p-0">Desfazer</button>
                </p>

                <label asp-for="BannerImageFile" class="control-label"></label>
                <input asp-for="BannerImageFile" class="form-control" id="BannerImageFileInput" />
                <span asp-validation-for="BannerImageFile" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Game.Plays" class="control-label"></label>
                <input asp-for="Game.Plays" class="form-control" />
                <span asp-validation-for="Game.Plays" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}

    
    <script>
        function setupImageControl(fileInputId, previewWrapperId, previewImgId, removeBtnId, removedMsgId, undoBtnId, removeFlagId) {
            const fileInput = document.getElementById(fileInputId);
            const previewWrapper = document.getElementById(previewWrapperId);
            const previewImg = document.getElementById(previewImgId);
            const removeBtn = document.getElementById(removeBtnId);
            const removedMsg = document.getElementById(removedMsgId);
            const undoBtn = document.getElementById(undoBtnId);
            const removeFlag = document.getElementById(removeFlagId);

            // Selecionar novo ficheiro -> mostra preview dele e cancela remoção
            fileInput.addEventListener('change', function () {
                if (fileInput.files && fileInput.files[0]) {
                    const reader = new FileReader();
                    reader.onload = function (e) {
                        previewImg.src = e.target.result;
                        previewWrapper.style.display = '';
                        removedMsg.style.display = 'none';
                        removeFlag.value = 'false';
                    };
                    reader.readAsDataURL(fileInput.files[0]);
                }
            });

            // Clicar em remover -> esconde preview, marca flag, limpa input de ficheiro
            removeBtn.addEventListener('click', function () {
                previewWrapper.style.display = 'none';
                removedMsg.style.display = '';
                removeFlag.value = 'true';
                fileInput.value = '';
            });

            // Desfazer remoção
            undoBtn.addEventListener('click', function () {
                previewWrapper.style.display = '';
                removedMsg.style.display = 'none';
                removeFlag.value = 'false';
            });
        }

        setupImageControl('CoverImageFileInput', 'coverPreviewWrapper', 'coverPreview', 'removeCoverBtn', 'coverRemovedMsg', 'undoRemoveCoverBtn', 'RemoveCoverImageInput');
        setupImageControl('BannerImageFileInput', 'bannerPreviewWrapper', 'bannerPreview', 'removeBannerBtn', 'bannerRemovedMsg', 'undoRemoveBannerBtn', 'RemoveBannerImageInput');
    </script>
}
